db:
  host: localhost
  port: 5432
  db-name: api
  username: cris6h16
  password: 12345678
  hikari:
    schema: test

jpa:
  show-sql: true
  properties:
    hibernate:
      format_sql: true
      ddl-auto: create


spring:
  config:
    import:
      - classpath:base.yaml
      - classpath:jwt.yaml
      - classpath:error-messages.yaml

#logging:
#    level:
#      root: trace