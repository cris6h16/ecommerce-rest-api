db:
  host: localhost
  port: 5432
  db-name: api
  username: cris6h16
  password: 12345678
  hikari:
    schema: prod

jpa:
  show-sql: true
  properties:
    hibernate:
      format_sql: true
      ddl-auto: create


spring:
  config:
    import:
      - classpath:base.yaml
      - classpath:mail.yaml
      - classpath:error-messages.yaml


logging:
  level:
    #      org:
    #        hibernate:
    #            SQL: debug
    root: debug