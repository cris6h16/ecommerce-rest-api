spring:
  application:
    name: restful-api-jwt

  config:
    import:
      - classpath:error-messages.yaml

  servlet:
    multipart:
      max-file-size: 5MB

  sql:
    init:
      mode: never

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080 # modificar docker-compose si se cambia

jwt:
  secret-key: 12345678yt432112345678765432123456789876543212345678432
  refresh-token-exp-minutes: 43200 # 30 days
  access-token-exp-minutes: 15

logging:
  level:
    root: error